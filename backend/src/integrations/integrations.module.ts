import { Module } from '@nestjs/common';

// Provider-specific adapters are registered here only after their terms,
// data location and failure policy have been approved by a feature prompt.
@Module({})
export class IntegrationsModule {}
